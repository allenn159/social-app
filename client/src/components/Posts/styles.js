import { makeStyles } from "@material-ui/core/styles";

export default makeStyles((theme) => ({
  paper: {
    width: "700px",
    minHeight: "150px",
    maxHeight: "400px",
    margin: "25px 0",
    textDecoration: "none",
  },
  postCont: {
    marginLeft: "25px",
  },
}));
